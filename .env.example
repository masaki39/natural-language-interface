# Copy to .env (gitignored) and fill in one key. Shell variables override this file.
# OpenRouter: model ~typesafe/jev-latest via /api/alpha/decisions
OPENROUTER_API_KEY=
# TypeSafe direct: https://console.typesafe.ai/settings/keys
TYPESAFE_API_KEY=
# Both set → OpenRouter. Force one with: openrouter | typesafe
# NLI_BACKEND=
# NLI_MODEL=
